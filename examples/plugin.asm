cli
sal bl,7
inc eax
sti
dec dx
rol ebx,1
jmp [ecx]
clc
sub [rax],rdi
sbb ch,ch
dec edx
mul bx
ret
sbb cx,cx
lock
imul bh
sub bx,bx
pause
rcl edx,1
idiv al
ret
.kPHs6Cqf8q763923632:
call [edi]
call [esi]
syscall
scasb
shr ax,5
cli
and [rdx],rax
add al,78
sal bl,3
cwd
xor rbp,50
jmp [eax]
scasw

dec al
test cl,cl
and [eax],ecx
lodsb
inc al
inc rbx
test cx,cx
cli
add [ecx],eax
jmp [eax]
xor bl,94
dec bh
call [ebp]
and [ecx],edi
clc
rcr dh,4
shl ch,3
cmpsw
std
idiv bx
shr bl,6
dec rbx
std
pause
lodsb
lock
pause
stc

call [esi]
jmp [eax]
cld
cmp [ebx],edi
std
mov bl,95
jmp [ecx]
test ax,84
jmp [ebx]
rcl rdi,1
scasw
pause
call [esi]
xor bh,59
add bh,bh
stc
sti
inc ecx
cli
xor dl,96
rol dh,6
call [ebp]
cmp eax,edi
neg cx
sbb [rbx],rbp
dec rcx
clc
lock
sbb bl,bl
add [ebp],edi
sbb cl,47
adc [ebx],ebp
mov [eax],ecx
zkKA602017463:
rcr ax,2
lodsw
call [edi]
adc [rdx],rax
shr rbp,8
ujHj56436163:
add [rax],rdx
shr bx,5
sar dh,3
idiv rsi
rcr rdi,5
cld
lahf
inc ebx
lodsb
hlt
test rax,rsi
call [edi]
ret
sbb ch,ch
cmp rcx,rax
jmp [ebx]
ror rsi,1
mul bl
sbb [ecx],esi
ror ch,3
xor cx,cx
neg rdi
ret
nop
or dl,61
idiv rbp
rcl ebx,1
test ah,ch
rol dl,4
sar rax,5
or cx,62

add dl,cl
call [esi]
sar rsi,7
rdtsc
mov [ecx],edx
call [edx]
inc eax
add [rsi],rbx
cld
mul bh
cmpsw
test bx,8
xor [edi],ebp
jmp [esi]
sbb [rbp],rbx
std
sal ch,2
xor [edx],eax
sar rbx,3
std
pause
dec ch
jmp [eax]
or al,bl
mul dl
ret
mul dl
cwd
std
scasw
cmp dx,dx
call [edi]
call [eax]
adc rbp,rdx
mul edx
add bh,bh
sar bl,8

call [edx]
dec rbx
test ah,ah
add ch,38
cmpsw
sti
jmp [esi]
rcl cl,1
stc
idiv ch
sar edx,5
sar bl,3
sti
lock
div rax
lodsb
call [edx]
cmp ah,ah
wait
idiv rax
scasw
jmp [edi]
rol rdi,8
sar rbp,5
lock
adc bh,bh
idiv ah
syscall
add ebx,3
scasw
ret
mul dl
sal cl,3
add rax,19
ror rdi,4
and [ecx],ebp
idiv cx
mul rbp
add edi,73
syscall
call [ebx]
cmp cl,cl
mul bx
mul bh
mov [ecx],ebp
adc ecx,62
sal ax,1
rcr rcx,7
dec rax
test rdi,3
and al,81
cmpsw
jmp [edi]
shr ecx,4
lock
call [edx]
shl rax,4
nop
pause
mov dx,ax
rcr rcx,3
cli
jmp [ebx]
idiv ax
cmpsw
add bl,bl
sal ebp,7
call [edi]
scasb
test edi,esi