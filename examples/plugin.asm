jmp [eax]
idiv dh
inc bl
jmp [ebp]
dec ax
and ebp,11
inc dl
dec cl
or [rdx],rsi
jmp [edx]
lahf
lodsb
sal edi,5
neg cl
BB9tYrp721464995:
cwd
pause
call [edx]
lodsb
jmp [ebp]
hlt
hlt
shl rbx,3
neg dl
clc
test [rdx],rdi
dec ah
ret
add edi,edx
rol al,2
cmpsw
div dl
and [rbp],rcx
test [ebx],edi
cwd
wait
rdtsc
and [rax],rcx
sub al,90

ror rdi,4
lodsb
call [ecx]
div edx
std
or [esi],ecx
sar rcx,2
ror dx,8
xor [ecx],ebx
ret
rcl bl,8
call [edi]
jmp [ebx]
cmp ah,ah
add dh,dh
std
rol ah,6
sub al,al
sub ecx,ebx
std
sar ecx,8
add [rdx],rcx
test bl,bl
rcl rbp,3
and ah,72
call [esi]
rcr bh,7
cmp rcx,rcx
neg rax
xor ebx,15
rol ebp,3
call [edi]
dec dx
dec ch
div rbp
cwd
sal rax,8
neg bh
rcr dx,1
sar rbx,8
rol cx,5
lodsb
adc dx,cx
dec ax
mul rdi
adc dl,32
cmp [edi],ebx
cmp al,dl
std
ret
sar ecx,5
lodsw
sbb esi,ecx
nop
hlt
or [ebx],edi
lodsb
rcr ebp,7
mov bl,al
sbb rcx,10
jmp [ebx]
lock
imul ax
sar rdi,7
rcl dx,8
lodsb
div cx
idiv dl
inc rdx
shr ah,8
adc ah,ah
rol rcx,4
jmp [ecx]
rol ebp,1
rcl cl,3
and cx,55
rol rdx,6
or bx,45
lodsb

scasw
sal dx,4
sub dx,77
sti
cli
xor bh,55
shl cx,6
shl ebx,1
sbb ch,18
ror rbp,6
rcl rcx,8
lodsw
sar ah,4
jmp [ebp]
cmpsw
lock
rcr ah,6
shl cx,1
adc dl,dl
neg ah
rcl esi,6
cli
or ebx,ecx
and bl,51
nop
rcl edi,4
yhqZmkP490171519:
.t3RyzuNe2S171883104:
call [edx]
sbb rdx,30
hlt
pause
or rcx,92
lodsb
rcl rcx,2
and ch,ch
scasw
sub [edi],esi
ret

syscall
rcr eax,2
xor ah,ah
call [ebx]
cmpsw
adc [edi],edx
sal bh,1
jmp [ecx]
jmp [esi]
lodsb
sbb [rbp],rcx
ret
nop
neg rbp
hlt
stc
cli
div bl
dec edx
cli
ror dl,2
neg dx
inc cl
mul edx
dec cx
xor rdi,rbp
div edi
sbb al,al
jmp [ebx]
call [ebx]
cld
inc ah
dec rcx
call [eax]
sal rsi,1
lahf
rol cx,5
shr dh,4
jmp [eax]
syscall
ror ebx,3
e13Zeonhj327205497:
jmp [ecx]
add ax,ax
imul ah
jmp [ebp]
add bx,44
mov bh,bh
or dx,38
.sGwRzq8q620139218:
cld
ret
imul bl
rdtsc
ret
hlt
scasb
shl edx,7
test rax,rcx
nop
add edi,edi
sbb [rbp],rbx
cmp bl,75
mul rsi
stc
dec rax
test [rdx],rcx
sar rcx,8
and cl,cl
shr dx,5
imul rbx
call [edx]
mov [rdx],rsi
cmpsw
shl ebx,1
scasw
neg ch
hlt
clc
syscall
neg dx
idiv rbp
rcr dl,3
call [ebx]
lodsw
lodsb
div ebp
imul bx
mul ax
lock

cli
sal bl,7
inc eax
sti
dec dx
rol ebx,1
jmp [ecx]
clc
sub [rax],rdi
sbb ch,ch
dec edx
mul bx
ret
sbb cx,cx
lock
imul bh
sub bx,bx
pause
rcl edx,1
idiv al
ret
.kPHs6Cqf8q763923632:
call [edi]
call [esi]
syscall
scasb
shr ax,5
cli
and [rdx],rax
add al,78
sal bl,3
cwd
xor rbp,50
jmp [eax]
scasw

dec al
test cl,cl
and [eax],ecx
lodsb
inc al
inc rbx
test cx,cx
cli
add [ecx],eax
jmp [eax]
xor bl,94
dec bh
call [ebp]
and [ecx],edi
clc
rcr dh,4
shl ch,3
cmpsw
std
idiv bx
shr bl,6
dec rbx
std
pause
lodsb
lock
pause
stc

call [esi]
jmp [eax]
cld
cmp [ebx],edi
std
mov bl,95
jmp [ecx]
test ax,84
jmp [ebx]
rcl rdi,1
scasw
pause
call [esi]
xor bh,59
add bh,bh
stc
sti
inc ecx
cli
xor dl,96
rol dh,6
call [ebp]
cmp eax,edi
neg cx
sbb [rbx],rbp
dec rcx
clc
lock
sbb bl,bl
add [ebp],edi
sbb cl,47
adc [ebx],ebp
mov [eax],ecx
zkKA602017463:
rcr ax,2
lodsw
call [edi]
adc [rdx],rax
shr rbp,8
ujHj56436163:
add [rax],rdx
shr bx,5
sar dh,3
idiv rsi
rcr rdi,5
cld
lahf
inc ebx
lodsb
hlt
test rax,rsi
call [edi]
ret
sbb ch,ch
cmp rcx,rax
jmp [ebx]
ror rsi,1
mul bl
sbb [ecx],esi
ror ch,3
xor cx,cx
neg rdi
ret
nop
or dl,61
idiv rbp
rcl ebx,1
test ah,ch
rol dl,4
sar rax,5
or cx,62
add dl,cl
call [esi]
sar rsi,7
rdtsc
mov [ecx],edx
call [edx]
inc eax
add [rsi],rbx
cld
mul bh
cmpsw
test bx,8
xor [edi],ebp
jmp [esi]
sbb [rbp],rbx
std
sal ch,2
xor [edx],eax
sar rbx,3
std
pause
dec ch
jmp [eax]
or al,bl
mul dl
ret
mul dl
cwd
std
scasw
cmp dx,dx
call [edi]
call [eax]
adc rbp,rdx
mul edx
add bh,bh
sar bl,8
call [edx]
dec rbx
test ah,ah
add ch,38
cmpsw
sti
jmp [esi]
rcl cl,1
stc
idiv ch
sar edx,5
sar bl,3
sti
lock
div rax
lodsb
call [edx]
cmp ah,ah
wait
idiv rax
scasw
jmp [edi]
rol rdi,8
sar rbp,5
lock
adc bh,bh
idiv ah
syscall
add ebx,3
scasw
ret
mul dl
sal cl,3
add rax,19
ror rdi,4
and [ecx],ebp
idiv cx
mul rbp
add edi,73
syscall
call [ebx]
cmp cl,cl
mul bx
mul bh
mov [ecx],ebp
adc ecx,62
sal ax,1
rcr rcx,7
dec rax
test rdi,3
and al,81
cmpsw
jmp [edi]
shr ecx,4
lock
call [edx]
shl rax,4
nop
pause
mov dx,ax
rcr rcx,3
cli
jmp [ebx]
idiv ax
cmpsw
add bl,bl
sal ebp,7
call [edi]
scasb
test edi,esi