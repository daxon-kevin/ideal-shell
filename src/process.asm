div bx
call [esi]
neg ax
sub ax,73
wait
shl edi,2
stc
call [ebx]
shr ch,2
mov rax,57
adc al,61
cmpsw
sub ax,86
test dh,35
pause
mov rbp,rbp
ret
nop
std
call [ecx]
inc dh
cmp bh,87
imul al
idiv dx
or al,al
wait

jmp [ebp]
sbb [esi],edx
sub dx,dx
sal eax,8
add cl,cl
shl dh,8
idiv bl
sub rsi,rax
rcl rax,8
imul edx
rdtsc
call [edi]
wait
div rcx
ror ch,1
mov dx,18
lodsb
dec cx
and rbp,rdx
inc ebp
and edx,ecx
test ax,79
and dh,dh
div edi
rcl bl,7
sub [rdx],rcx
std
shl eax,7
lodsb
xor rax,27
ret
ret
cli
mov [rcx],rcx
cmp bh,70
lodsb
.uv3FNsYc524030725:
dec ah

rcl bl,1
jmp [ecx]
sal esi,7
jmp [esi]
ret
nop
and [ebx],edx
mul ebp
adc rax,50
lahf
jmp [edi]
test [rsi],rdx
rcr bx,1
ret
syscall
call [ecx]
call [ebx]
and bl,bl
rcl rbp,8
ret
ret
sbb rsi,rax
cld
call [edi]
cmp [rsi],rcx
rdtsc
sal ax,2
div dx
jmp [ebx]
or eax,47
shr al,8
nop
div edi
sbb [edi],eax
cld
sar ebx,5
xor eax,esi
mul rsi
or bh,4
mul cl
test rbp,rcx
nop
shl ah,5
add cx,6
jmp [eax]
rcr ebx,4
wait
mov [rbp],rax
imul edi

shl edx,3
call [eax]
inc bl
jmp [ecx]
call [edi]
adc dl,5
inc ebp
cmpsw
add [rdx],rcx
idiv ch
sar rsi,7
test rdi,rdi
rcl ax,8
mul cl
div rdx
cmpsw
neg rcx
.i870517191:
sti
mov [edi],edx
rdtsc
scasw
call [ebx]
sar ah,6
imul dx
idiv ah
cmpsw
idiv rbp
rol rdx,5
sub cl,cl
scasb
sar rax,2
hlt
add dx,dx
imul al
rcr eax,2
test [eax],ebp
rcl dl,5
jmp [edx]
sal dl,7
shl ebx,3
sar rsi,8
rol cx,1
ror dx,8
hlt
lock
add eax,51
shl rsi,6
inc cl
std
call [edx]
nop
cmpsw
syscall
div edx
imul ah
std
sub dh,38
lodsw
cmpsw
ret
call [edx]
dec rax
sbb bx,bx
