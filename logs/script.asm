rol ah,5
wait
rol ax,5
adc rdx,20
div rbp
shl ch,5
std
jmp [eax]
sub [rcx],rdx
rdtsc
rol ebx,8
shl ch,2
neg rdx
.yfR8w590639981:
cli
and dh,26
stc
rcl edx,1
sar edx,8
jmp [ecx]
ret
call [esi]
jmp [edi]
div bx
and bh,78
sal dl,8
std
call [edx]
rdtsc
rcl ebp,2
nop
jmp [eax]
mov rbp,rbp
rdtsc
call [edi]
cmp [edx],eax
std
imul ebp
call [esi]
mul edi
mul ax
cmp rdx,42
lodsb
and bl,84
scasb
lodsw
nop
xor rcx,rbx
neg al
div cl
nop
or [rbp],rdi
cmpsw
ret
sub [ebp],edx
std
rol dx,7
shl edx,7
sar rcx,5
jmp [ebx]
scasb
cld
imul rbx
shl ebx,3
