xor bh,bh
shl rax,2
shr dl,6
stc
shr rdi,3
ret
rcl cx,5
idiv ebp
cld
jmp [eax]
pause
pause
lodsb
add cl,34
std
div dh
shr rdi,6
lock
call [eax]
rol al,3
hlt
syscall
rcr ch,2
or bx,52
idiv rsi
rdtsc
sbb [edi],esi
lodsb
rcr bx,8
rcr ecx,5
xor [rsi],rax
sar rdi,3
jmp [edx]
jmp [ebx]
sal rdi,4

call [ebp]
jmp [ebx]
lock
lahf
std
jmp [edi]
inc esi
shl al,8
shl rbx,3
call [edi]
call [edi]
rol ebx,5
rcl ax,5
rcr ch,2
rcl bh,8
add [esi],ecx
ret
neg dl
call [edi]
xor bl,bl
xor cx,89
rcl al,6
rol rax,6
dec rsi
cmpsw
std
add dx,dx

cld
adc cl,cl
shr ecx,8
idiv ebp
jmp [edi]
syscall
or [eax],edi
div ebx
imul cl
and [rcx],rdi
jmp [ecx]
stc
wait
idiv rax
nop
mul dx
jmp [esi]
sti
shl bl,4
rcl bx,5
sti
jmp [ebp]
lodsw
idiv ch
shl ecx,5

adc [rbp],rcx
clc
call [ebp]
scasw
cli
rcr dx,4
test cx,cx
rol al,8
sub ecx,99
wait
jmp [edx]
lodsb
.IWjNt737373300:
inc edx
ret
sbb al,al
and esi,16
rol rdi,6
sar rdi,4
test ebx,88
syscall
call [esi]
sti
test [rdi],rbp
syscall
ret
scasw
OQT370487205:
mul rbp
std
xor rsi,80
lodsw
mov al,59
div rbx
sbb dl,al
stc
rdtsc
hlt
and ch,ch
sub cx,2
adc rcx,rdx
imul ch
ret
lock
sbb bh,dh
ROC84686561:
wait
and [ecx],esi
shr rcx,6
test rax,rdi
jmp [edx]
and bx,bx
scasb
call [ebx]
shr ch,5
cmp al,96
add [ecx],ebx
.DwlmP6773338771:
adc dx,58
sub ebp,95
nop
or edx,32
rcr bl,2
sti
hlt
.bw2830807047:
sub [rbx],rbp
pause
jmp [esi]
dec cl
lodsb
lodsw
lodsw
lodsb
imul rbp
sub [esi],edx
adc edi,edx
add [rax],rax
adc [edi],esi
cld
cli
sbb bh,78
imul edi
jmp [eax]
ror rsi,5
cld
adc edx,ecx
imul bh
sub rsi,rdx
rcl cx,1
rcr rdi,6
ret
shr bx,1
call [ecx]
ret
jmp [ecx]
mul edi
call [ecx]
jmp [ebx]
wait
mov ch,ch
lahf
ret
lodsb
stc
idiv dl
lodsb
rcl rdi,5
lock
cmp al,al
mul bl

div bx
call [esi]
neg ax
sub ax,73
wait
shl edi,2
stc
call [ebx]
shr ch,2
mov rax,57
adc al,61
cmpsw
sub ax,86
test dh,35
pause
mov rbp,rbp
ret
nop
std
call [ecx]
inc dh
cmp bh,87
imul al
idiv dx
or al,al
wait
jmp [ebp]
sbb [esi],edx
sub dx,dx
sal eax,8
add cl,cl
shl dh,8
idiv bl
sub rsi,rax
rcl rax,8
imul edx
rdtsc
call [edi]
wait
div rcx
ror ch,1
mov dx,18
lodsb
dec cx
and rbp,rdx
inc ebp
and edx,ecx
test ax,79
and dh,dh
div edi
rcl bl,7
sub [rdx],rcx
std
shl eax,7
lodsb
xor rax,27
ret
ret
cli
mov [rcx],rcx
cmp bh,70
lodsb
.uv3FNsYc524030725:
dec ah

rcl bl,1
jmp [ecx]
sal esi,7
jmp [esi]
ret
nop
and [ebx],edx
mul ebp
adc rax,50
lahf
jmp [edi]
test [rsi],rdx
rcr bx,1
ret
syscall
call [ecx]
call [ebx]
and bl,bl
rcl rbp,8
ret
ret
sbb rsi,rax
cld
call [edi]
cmp [rsi],rcx
rdtsc
sal ax,2
div dx
jmp [ebx]
or eax,47
shr al,8
nop
div edi
sbb [edi],eax
cld
sar ebx,5
xor eax,esi
mul rsi
or bh,4
mul cl
test rbp,rcx
nop
shl ah,5
add cx,6
jmp [eax]
rcr ebx,4
wait
mov [rbp],rax
imul edi

shl edx,3
call [eax]
inc bl
jmp [ecx]
call [edi]
adc dl,5
inc ebp
cmpsw
add [rdx],rcx
idiv ch
sar rsi,7
test rdi,rdi
rcl ax,8
mul cl
div rdx
cmpsw
neg rcx
.i870517191:
sti
mov [edi],edx
rdtsc
scasw
call [ebx]
sar ah,6
imul dx
idiv ah
cmpsw
idiv rbp
rol rdx,5
sub cl,cl
scasb
sar rax,2
hlt
add dx,dx
imul al
rcr eax,2
test [eax],ebp
rcl dl,5
jmp [edx]
sal dl,7
shl ebx,3
sar rsi,8
rol cx,1
ror dx,8
hlt
lock
add eax,51
shl rsi,6
inc cl
std
call [edx]
nop
cmpsw
syscall
div edx
imul ah
std
sub dh,38
lodsw
cmpsw
ret
call [edx]
dec rax
sbb bx,bx